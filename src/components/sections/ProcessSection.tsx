import React from 'react';
import { Upload, Sparkles, BookOpen, ClipboardList, Users, Recycle, Camera } from 'lucide-react';
import { IconShieldLock } from '@tabler/icons-react';

// ------------------------------
// Reusable Card component for each process step
// ------------------------------
const ProcessCard = ({ step, title, description, icon: Icon, index, isLast }) => {
  return (
    <div className="relative flex flex-col items-center group">
      {/* Icon Container */}
      <div className="relative z-10 mb-6">
        <div className="w-20 h-20 rounded-2xl bg-gradient-to-br from-emerald-400 to-green-500 dark:from-emerald-500 dark:to-green-600 flex items-center justify-center shadow-lg shadow-emerald-500/30 dark:shadow-emerald-500/20 transition-all duration-300 group-hover:scale-110 group-hover:shadow-xl group-hover:shadow-emerald-500/40">
          <Icon className="w-10 h-10 text-emerald-50 dark:text-emerald-100" strokeWidth={2.5} />
        </div>
        {/* Step Badge */}
        <div className="absolute -top-2 -right-2 w-8 h-8 bg-white dark:bg-gray-800 rounded-full flex items-center justify-center text-emerald-600 dark:text-emerald-400 font-bold text-sm shadow-md border-2 border-emerald-400 dark:border-emerald-500">
          {step}
        </div>
      </div>

      {/* Card Content */}
      <div className="bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 border border-gray-200 dark:border-gray-700 hover:border-emerald-500 w-full h-full">
        <h3 className="text-2xl font-bold text-gray-900 dark:text-white mb-3 text-center group-hover:text-emerald-500 dark:group-hover:text-emerald-400 transition-colors duration-300">
          {title}
        </h3>
        <p className="text-gray-600 dark:text-gray-300 leading-relaxed text-center">{description}</p>
      </div>

      {/* Dotted connecting lines */}
      {/* Horizontal for same row (max 4 per row) */}
      {!isLast && index % 4 !== 3 && (
        <div className="absolute top-10 right-[-50%] w-full h-0.5 border-t-2 border-dotted border-emerald-400 dark:border-emerald-500" />
      )}
      {/* Vertical to next row if last card in row */}
      {!isLast && index % 4 === 3 && (
        <div className="absolute bottom-[-2rem] left-1/2 w-0.5 h-8 border-l-2 border-dotted border-emerald-400 dark:border-emerald-500" />
      )}
    </div>
  );
};

// ------------------------------
// Main Process Section Component
// ------------------------------
export default function ProcessSection() {
  const steps = [
    { step: 1, title: "Upload", description: "Capture the damage: Upload a photo of your damaged item.", icon: Upload },
    { step: 2, title: "Analyze", description: "AI processes the image to detect item, material, and damage level.", icon: Sparkles },
    { step: 3, title: "Guide", description: "Receive a step-by-step repair guide generated by AI.", icon: BookOpen },
    { step: 4, title: "Verify", description: "Double-check repair instructions for accuracy.", icon: ClipboardList },
    { step: 5, title: "Community Support", description: "Share or ask questions in the community.", icon: Users },
    { step: 6, title: "Eco Contribution", description: "Track environmental impact and points earned.", icon: Recycle },
    { step: 7, title: "Security", description: "Your data is protected end-to-end.", icon: IconShieldLock },
    { step: 8, title: "Advanced Insights", description: "AI analytics provide advanced repair insights.", icon: Camera }
  ];

  return (
    <section className="min-h-screen py-20 px-4">
      <div className="max-w-7xl mx-auto">

        {/* Section Header */}
        <div className="text-center mb-16">
          <h2 className="text-4xl md:text-5xl font-bold text-gray-900 dark:text-white mb-4 transition-colors duration-300">
            How It <span className="text-emerald-500 dark:text-emerald-400">Works</span>
          </h2>
          <p className="text-lg text-gray-600 dark:text-gray-300 max-w-2xl mx-auto transition-colors duration-300">
            Step-by-step AI-powered repair process. Modern, clean, and premium design.
          </p>
        </div>

        {/* Grid Steps */}
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-12 relative">
          {steps.map((stepData, index) => (
            <ProcessCard
              key={stepData.step}
              {...stepData}
              index={index}
              isLast={index === steps.length - 1}
            />
          ))}
        </div>

        {/* Terminal placeholder */}
        <div className="mt-20 p-6 bg-gray-100 dark:bg-gray-900 rounded-lg font-mono h-40 w-full text-gray-800 dark:text-gray-100">
          {/* You can put your terminal UI or code block here */}
          <p>$ Terminal placeholder for future logs or AI output...</p>
        </div>

        {/* CTA Button */}
        <div className="text-center mt-16">
          <button className="px-8 py-4 bg-gradient-to-r from-emerald-500 to-green-600 dark:from-emerald-600 dark:to-green-700 text-white rounded-xl font-semibold shadow-lg shadow-emerald-500/30 dark:shadow-emerald-500/20 hover:shadow-xl hover:shadow-emerald-500/40 dark:hover:shadow-emerald-500/30 transform hover:scale-105 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:ring-offset-2 dark:focus:ring-offset-gray-900">
            Start Your Repair Journey
          </button>
        </div>
      </div>
    </section>
  );
}
